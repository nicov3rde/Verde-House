# Path to the clip-bot directory (default: ../clip-bot relative to consumer-app)
CLIP_BOT_DIR=../clip-bot

# Where job data is stored (default: backend/data)
DATA_DIR=data

# Port (default: 8000)
PORT=8000
